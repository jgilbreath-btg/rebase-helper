[project]
name = "rebase-helper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gitpython>=3.1.57",
]

[tool.pyright]
typeCheckingMode = "strict"
